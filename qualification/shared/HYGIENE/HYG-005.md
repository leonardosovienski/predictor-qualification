# HYG-005 — preservação do checkout sujo do CAIN (D-2)

**Critério:** em `C:\QUALIFICACAO\preserve\cain-24f784c-20260922\`: `git status`, `git diff` (patch
dos rastreados), cópia dos não rastreados e `git bundle` verificado; `C:\CAIN` intocado.

**Resultado:** cumprido. A preservação está completa e verificada. Em `C:\CAIN` nenhum conteúdo
mudou: o único efeito observado foi a atualização do mtime de 17 objetos git que já existiam (ver a
seção "Escrita detectada" e o exame posterior). Decisão do dono: examinar os objetos e apagá-los só
se fossem inúteis. Eles estão em uso, então nada foi apagado.

## Estado encontrado

`C:\CAIN\projeto`: HEAD destacado em `24f784c5dde1fa66c262ad5899f4fd8d02526adf`, **17** caminhos
sujos (10 rastreados modificados + 7 não rastreados), e não 14 como o `HYGIENE.json` descreve. Os 17
foram preservados. A lista está em `git_status_porcelain.txt` dentro da pasta de preservação.

## Conteúdo preservado (local, não publicado; pode conter dados privados, D-2)

| Artefato | sha256 |
|---|---|
| `git_status.txt` | `b2228d24dec12a7448548c054ac61372dfceb3b05f8d1d81a6f87425c0b97967` |
| `git_status_porcelain.txt` | `4e3e86fc2c807719c4aecba1d33f55be4f3ee78f7e1edbf772dfe0f8a922e459` |
| `tracked_changes.patch` (`git diff --binary HEAD`) | `4de25ff1a911bfd6a1a15b33cf7bb06ac7a80b7b7e0800a1a86d1591a4d89075` |
| `tracked_changes.stat.txt` | `46302826f147f622bbb9204817c029b966c51e212fe8b98585b192156d350aa3` |
| `cain-all.bundle` (`git bundle create HEAD --all`) | `125358a9bdbca2bc08ccfe8b9c3a05e0e80610000fdc24c4d4e43606fe551a6c` |
| `untracked/` (7 arquivos) e `tracked_worktree/` (10 arquivos, cópia byte a byte) | no `MANIFEST.sha256` |
| `MANIFEST.sha256` (25 arquivos) | `8a5a522db60604970be191b22bbc2c50f10cedf193893d562405b367f314ff47` |

`tracked_worktree/` existe porque o patch normaliza fim de linha (`.gitattributes` do CAIN:
`* text=auto eol=lf`). A cópia guarda os bytes exatos dos 10 arquivos modificados.

## Verificação

- `git bundle verify`: *okay*, 43 refs, *complete history*.
- Clone do bundle + checkout de `24f784c` + `git apply --check tracked_changes.patch`: exit 0.
- Comparação byte a byte das 17 cópias com os originais: 17/17 iguais.
- Fingerprint de `C:\CAIN\projeto` antes = depois (HEAD, sha256 de `.git/index`, `.git/HEAD`,
  `.git/packed-refs`, hash de todas as refs e sha256 dos 17 arquivos sujos).
- Todos os comandos git rodaram com `GIT_OPTIONAL_LOCKS=0` (sem reescrita do index).

## Escrita detectada em `C:\CAIN\projeto\.git\objects`

`find C:/CAIN/projeto -newer <marcador>` encontrou **17 objetos soltos com mtime novo** (15 blobs + 2 trees),
todos com mtime `2026-09-23T03:41:42Z`. Os 15 blobs são os próprios conteúdos dos arquivos sujos. Não
entrou dado novo: refs, HEAD, index e árvore de trabalho estão iguais
(fingerprint). `C:\CAIN` no nível superior: nada novo.

Causa **provável, não provada**: o harness da sessão do agente. A mesma sequência de comandos,
repetida numa cópia fiel (clone do bundle + o mesmo estado sujo), cria 0 objetos. Já o repositório
de evidência ganhou 28 objetos soltos às 03:26:50, no início da sessão e antes do primeiro comando do
agente (03:36:01). Um `cd C:\CAIN\projeto` do agente fez a sessão adotar esse diretório de trabalho
às 03:41:4x. O exame posterior (abaixo) mostrou que os objetos já existiam e estão em uso.
Registrado como HF-004 em `FINDINGS.md`. Correção de processo: o agente não usa mais `cd` em
diretórios de `C:\CAIN` (só `git -C` com `GIT_OPTIONAL_LOCKS=0`).

### Exame posterior (decisão do dono: "se for inútil apaga, se for bom usa e aproveita")

Só leitura, com `GIT_OPTIONAL_LOCKS=0`:

- `git rev-list --objects --all --reflog --indexed-objects`: **os 17 objetos são alcançáveis** (17/17).
- Quem referencia: os índices de **10 worktrees** do CAIN (`C:\CAIN\work\...` e
  `C:\CRIPTO\work\cain-cripto-integration-20260919\...`), com mtime de 15/09 a 21/09/2026, bem
  antes desta sessão. Os 2 trees são subárvores (`tools/`, `src/cain/llm/`) do cache-tree desses
  índices.
- Conclusão: os objetos **já existiam** e estão em uso. Às 03:41:42 só o mtime deles foi atualizado
  (o git "refresca" um objeto solto existente em vez de regravá-lo), sem conteúdo novo. Apagar
  corromperia esses 10 índices, então **nada foi apagado**.
- A causa exata do "refresh" não foi determinada. A hipótese continua sendo o harness da sessão,
  porque a sequência de comandos do agente, numa cópia fiel, não escreve objetos.

Logs brutos: `RAW_LOGS/HYG-005_preserve.log`, `RAW_LOGS/HYG-005_hf004_reachability.log`.
