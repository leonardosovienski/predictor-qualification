# HYG-003 — `cripto-predictor`: protocolo pela release, lock regenerado, V1 fora da coleta

**Critério:** `uv lock --check` e `uv sync --locked --all-extras` verdes; suíte coleta sem `cain`.

**Resultado:** cumprido no `main` = `5fd4e1b06c79231fd3d9485a1483f3b65e854bea` (merge de cripto#124 → `f8fb948`, depois cripto#125).

- `predictor-research-protocol` 1.0.3rc1 vem pela URL da release (sha256 `312ab974…`). `grep -c cain uv.lock` = 0, ou seja, sem `cain-research`.
- `tests/test_research_execution.py` e `tests/test_research_recovery.py` foram para `legacy/v1_integration/` (com README, D-13). `testpaths = ["tests"]`.
- **CI hospedado no `main`:** [run 35822807920](https://github.com/leonardosovienski/cripto-predictor/actions/runs/35822807920), `push`, `success`. `uv sync --locked --all-extras` nos jobs `quality` e `all-extras`; pytest em cada um: `1618 passed`, sem `cain` instalado. `python-314-experimental`: `1583 passed, 7 skipped`. O `container` passou, instalando pelo lock com `--require-hashes`.
- Junto (cripto#124): Dockerfile e contrato da wheel instalam pelo lock (parte Cripto de HYG-004), formatação sem tocar o arquivo congelado (HF-008), aiohttp/cryptography/ccxt atualizados por causa do Trivy (HF-009).
- Local (Windows, uv 0.12.18, cache em `C:\Cripto`): `uv lock --check` exit 0.

Logs brutos: `RAW_LOGS/HYG-007/cripto-predictor_run35822807920.{json,log}`, `RAW_LOGS/HYG-007_facts.txt`, `RAW_LOGS/post_merge_checks.log`.
