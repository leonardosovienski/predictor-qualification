# HYG-009 — `stocks-predictor`: `predictor-core>=3.2.1,<4` (D-7)

**Critério:** lock inalterado em 3.2.1.

**Resultado:** cumprido no `main` = `4e98a67b68c55888a4788663f3d5bf0899966cb4` (merge de stocks#91; stocks#92 → `7c846fe`).

- `pyproject.toml:13`: `"predictor-core>=3.2.1,<4"`.
- `git diff --stat 5cf27f44 HEAD -- uv.lock` vazio: o lock ficou **byte a byte igual** ao do baseline pré-higiene. O Core continua `3.2.1` pela URL da release, sha256 `10ef42f3…`.
- A evidência atual R8 (`docs/engineering/current-operational-evidence.json`) foi atualizada com o novo hash do `pyproject.toml`, como pede a regra local.
- **CI hospedado no `main`:** [run 35822899971](https://github.com/leonardosovienski/stocks-predictor/actions/runs/35822899971), `push`, `success`. `uv sync --locked --all-extras` em 3.13 e 3.14; pytest: `945 passed, 71 subtests passed` em cada. O verificador R8 passou dentro do job.
- Nada foi instalado no Python do Windows (regra do Stocks).

Logs brutos: `RAW_LOGS/HYG-007/stocks-predictor_run35822899971.{json,log}`, `RAW_LOGS/HYG-007_facts.txt`, `RAW_LOGS/post_merge_checks.log`.
