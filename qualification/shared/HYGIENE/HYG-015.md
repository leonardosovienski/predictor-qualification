# HYG-015 — workflows de integração V1 aposentados (D-13)

**Critério:** diff dos 7 workflows; run de push/PR sem esses workflows.

**Resultado:** cumprido nos 7 workflows (brasileirao#75 mergeado em `b2b096c`).

| Workflow | PR | Gatilhos no `main` | Cabeçalho D-13 |
|---|---|---|---|
| `stocks-predictor/.github/workflows/cain-export.yml` | stocks#92 | `workflow_dispatch` | sim |
| `cripto-predictor/.github/workflows/integration-audit.yml` | cripto#125 | `workflow_dispatch` | sim |
| `core-predictor/.github/workflows/consumer-compatibility.yml` | core#32 | `workflow_dispatch` | sim |
| `cain/.github/workflows/cain-supply-linux.yml` | cain#9 | `workflow_dispatch` (saiu o `paths:` inválido) | sim |
| `cain/.github/workflows/cain-completion.yml` | cain#9 | `workflow_dispatch` | sim |
| `ecosystem-predictor/.github/workflows/cain-crypto-integration.yml` | ecosystem#26 | `workflow_dispatch` | sim |
| `brasileirao-predictor/.github/workflows/cain-export.yml` | brasileirao#75 | `workflow_dispatch` | sim |

Nenhum arquivo foi apagado; nenhum pacote novo em BR ou Stocks.

**Runs de push sem os workflows aposentados** (`RAW_LOGS/HYG-015_push_runs.log`, commit HEAD de cada `main`):
cain só `CI`; cripto só `CI` (sem "Installed Crypto CAIN integration"); stocks só `CI Pipeline`
(sem "Cain report exporter"); core `CI` + CodeQL (sem "Pinned consumer compatibility");
ecosystem `CI` + "Full-history security regression"; brasileirao (`e14f339`) `CI Pipeline` + "Scoped synthetic
publication validation" (sem "Cain report exporter").

Logs brutos: `RAW_LOGS/HYG-015_push_runs.log`, `RAW_LOGS/post_merge_checks.log`, `RAW_LOGS/final_checks.log`.
