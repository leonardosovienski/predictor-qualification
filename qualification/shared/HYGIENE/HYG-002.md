# HYG-002 — `cain`: `tool.uv.sources`, `uv.lock`, sem `vendor/`

**Critério:** `uv lock --check` e `uv sync --locked` verdes.

**Resultado:** cumprido no `main` = `8b8915aa7c9053b8f078a9fba20b7029180d114c` (merge de cain#8 → `a5e4d25`, depois cain#9).

- `[tool.uv.sources]`: `predictor-research-{snapshot,bundle,protocol}` apontam para as URLs das releases de HYG-001; o `uv.lock` fixa os sha256 `3bd4c041…`, `65cf40c5…` e `312ab974…`, iguais aos assets publicados.
- `vendor/` removido (`ls vendor`: não existe). O passo de CI que usava `vendor/` foi trocado por `uv sync --locked`, e o `tools/verify_wheel.py` passou a usar `uv export --locked` com `--require-hashes`.
- `cain.__version__` alinhado ao `pyproject` (0.4.13rc4) (HF-006).
- **CI hospedado no `main`:** [run 35822857468](https://github.com/leonardosovienski/cain/actions/runs/35822857468), `push`, `success`. Python 3.11/3.12/3.13/3.14 com `uv lock --check` e `uv sync --locked --extra dev --extra vision`; pytest em cada versão: `920 passed, 2 skipped`.
- Local (Windows, uv 0.12.18): `uv lock --check` exit 0.

Logs brutos: `RAW_LOGS/HYG-007/cain_run35822857468.{json,log}`, `RAW_LOGS/HYG-007_facts.txt`, `RAW_LOGS/post_merge_checks.log`.
