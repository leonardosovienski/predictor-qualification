# HYG-008 — suíte do Ops em Linux × py3.13 e Windows × py3.13 (triagem da SHARED-003)

**Critério:** logs brutos; SHARED-003 registrada em `SHARED_ISSUES.json` com o estado observado.

**Resultado:** cumprido. `test_timeout_and_truncation` **não falhou** em nenhuma execução. A
SHARED-003 foi registrada como `OPEN_AWAITING_VERDICT` / `NOT_REPRODUCED_IN_TRIAGE`. O veredito
(C13) é da missão `crypto`.

## Código testado

`predictor-ops` `main` = `b19e69527c0fda5cb1f96281d3a984fea1431768`. Entre a tag `v4.2.1`
(`ddd91444…`, origem da wheel `da4fa540…`) e esse commit só mudou documentação: `src/`,
`tests_v2/`, `pyproject.toml` e `uv.lock` são idênticos (`git diff --stat` vazio).

## Execuções

| Ambiente | Como | Resultado | Log bruto |
|---|---|---|---|
| GitHub Actions `ubuntu-latest` × 3.13 | CI disparado por `workflow_dispatch` no `main`, [run 35821939095](https://github.com/leonardosovienski/predictor-ops/actions/runs/35821939095), `uv sync --locked` | 87 passed; `posix_integration` 1 passed | `RAW_LOGS/HYG-008/ops_ci_linux_py313.log` |
| GitHub Actions `windows-latest` × 3.13 | mesmo run | 87 passed; `windows_integration` 2 passed | `RAW_LOGS/HYG-008/ops_ci_windows_py313.log` |
| Windows 11 local × CPython 3.13.14 | `uv sync --locked --all-extras`; `pytest -W error::ResourceWarning` | 87 passed; `windows_integration` 2 passed | `RAW_LOGS/HYG-008/ops_local_windows_py313.log` |
| Windows 11 local × CPython 3.13.14 | coleta + `test_timeout_and_truncation` isolado 3× com `-v` | coletado (1 de 87); 3/3 PASSED | `RAW_LOGS/HYG-008/ops_local_windows_timeout_test.log` |

O CI não roda com `-v`. A presença do teste nos 87 aparece na coleta local (`tests_v2` é o único
`testpath`).

## Limites da triagem

Só a partir do source: a instalação pela wheel publicada fica para a missão `crypto`. Uma execução
por ambiente no CI, sem repetição em volume. Isso não permite classificar a falha (flaky,
environmental etc.), apenas registrar que ela não se reproduziu nestas execuções.
